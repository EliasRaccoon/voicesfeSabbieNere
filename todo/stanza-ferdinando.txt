Sprint n.1

Preparare il componente "FrontPage", che sarà la pagina di ingresso per i visitatori.

Il componente FrontPage deve presentare l'anteprima degli ultimi 10 post pubblicati.
Definire le interfacce per i post e i dati di prova all'interno della cartella del componente. Verranno
sostituiti coi dati veri provenienti dai servizi, ma sapete più o meno che forma dovranno avere.

Ogni anteprima deve presentare:
[titolo del post]
[prime tre righe]
di [nickblogger] pubblicato il [data e tempo]

Servirà sviluppare anche un componente di nome BlogPostPreview, che riceverà un BlogPost come input signal.

Non modificare nessun file se non il componente. Testarlo in app, poi fare restore di app allo stato precedente 
per evitare di incorrere in conflitti al momento del merge.

Lavorare su una branch di nome dev-front-page. Una volta terminato, loggarsi su git hub e fare la pull request